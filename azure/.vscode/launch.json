{
  "version": "0.2.0",
  "configurations": [
    {
      // F5 runs the API with the debugger attached. Breakpoints work in any
      // file under backend/.
      "name": "Apex API (azure :8002)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["main:app", "--reload", "--port", "8002"],
      "cwd": "${workspaceFolder}/backend",
      "envFile": "${workspaceFolder}/backend/.env.azure",
      "env": { "JIRA_MOCK_MODE": "true" },
      "console": "integratedTerminal",
      "justMyCode": false
    },
    {
      "name": "Apex Frontend (azure :3002)",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}/frontend",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev"],
      "console": "integratedTerminal"
    }
  ],
  "compounds": [
    {
      // Start both with one keystroke.
      "name": "Apex — full stack (azure)",
      "configurations": ["Apex API (azure :8002)", "Apex Frontend (azure :3002)"]
    }
  ]
}
